interface:
  display_name: "Lead Scoring"
  short_description: "Rank leads for booking-system outreach"
  default_prompt: "Use $lead-scoring to score my outreach CRM."

policy:
  allow_implicit_invocation: true
